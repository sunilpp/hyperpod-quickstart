# PyTorch DDP test on EKS — verifies multi-node GPU communication
# Requires: Kubeflow PyTorch Training Operator
#
# Usage: kubectl apply -f pytorchjob.yaml
# Check: kubectl get pytorchjobs
# Logs:  kubectl logs pytorch-ddp-test-master-0
#
# Works on both EFA (p5) and non-EFA (g5) instances.

apiVersion: kubeflow.org/v1
kind: PyTorchJob
metadata:
  name: pytorch-ddp-test
  namespace: default
spec:
  pytorchReplicaSpecs:
    Master:
      replicas: 1
      restartPolicy: OnFailure
      template:
        spec:
          containers:
            - name: pytorch
              image: 763104351884.dkr.ecr.us-west-2.amazonaws.com/pytorch-training:2.1.0-gpu-py310-cu121-ubuntu20.04-sagemaker
              command:
                - python3
                - -c
                - |
                  import torch
                  import torch.distributed as dist
                  import os

                  dist.init_process_group(backend='gloo')
                  rank = dist.get_rank()
                  world_size = dist.get_world_size()

                  torch.cuda.set_device(0)
                  device = torch.device('cuda:0')

                  tensor = torch.ones(1024, 1024, device=device) * rank
                  dist.all_reduce(tensor, op=dist.ReduceOp.SUM)

                  expected = world_size * (world_size - 1) / 2
                  actual = tensor[0][0].item()
                  print(f'Rank {rank}/{world_size}: Result={actual}, Expected={expected}')
                  if rank == 0:
                      print('SUCCESS' if abs(actual - expected) < 0.01 else 'FAILURE')

                  dist.destroy_process_group()
              resources:
                limits:
                  nvidia.com/gpu: 1
              env:
                - name: NCCL_NET_PLUGIN
                  value: none
    Worker:
      replicas: 1
      restartPolicy: OnFailure
      template:
        spec:
          containers:
            - name: pytorch
              image: 763104351884.dkr.ecr.us-west-2.amazonaws.com/pytorch-training:2.1.0-gpu-py310-cu121-ubuntu20.04-sagemaker
              command:
                - python3
                - -c
                - |
                  import torch
                  import torch.distributed as dist
                  import os

                  dist.init_process_group(backend='gloo')
                  rank = dist.get_rank()
                  world_size = dist.get_world_size()

                  torch.cuda.set_device(0)
                  device = torch.device('cuda:0')

                  tensor = torch.ones(1024, 1024, device=device) * rank
                  dist.all_reduce(tensor, op=dist.ReduceOp.SUM)

                  expected = world_size * (world_size - 1) / 2
                  actual = tensor[0][0].item()
                  print(f'Rank {rank}/{world_size}: Result={actual}, Expected={expected}')

                  dist.destroy_process_group()
              resources:
                limits:
                  nvidia.com/gpu: 1
              env:
                - name: NCCL_NET_PLUGIN
                  value: none
